/*
 * CardSkeleton Messages
 *
 * This contains all the text for the CardSkeleton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardSkeleton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardSkeleton component!',
  },
});
