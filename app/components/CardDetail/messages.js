/*
 * CardDetail Messages
 *
 * This contains all the text for the CardDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardDetail component!',
  },
});
